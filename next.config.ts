import type { NextConfig } from "next";

module.exports = {
  pageExtensions: ['tsx', 'ts', 'jsx', 'js'], // Garante que Next.js reconheça os arquivos TSX/TS
}

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
